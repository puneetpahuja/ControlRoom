(ns dtm.write
  (:require [dtm.util :as util]))


;;; ====================================tasks===================================

(defn task [task]
  (let []))

(defn tasks [_username tasks]
  (mapv task tasks))
